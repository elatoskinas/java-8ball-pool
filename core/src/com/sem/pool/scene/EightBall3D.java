package com.sem.pool.scene;

import com.badlogic.gdx.graphics.g3d.ModelInstance;

/**
 * Ball3D with id 8 which represents the 8 (black) ball.
 */
public class EightBall3D extends Ball3D {
    /**
     * Constructs a new 3D Pool EightBall instance with
     * the specified id and model.
     *
     * @param id    ID of the ball
     * @param model Model object of the ball
     */
    public EightBall3D(int id, ModelInstance model) {
        super(id, model);
    }
}
